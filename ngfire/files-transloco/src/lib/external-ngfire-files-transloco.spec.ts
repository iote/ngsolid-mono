import { externalNgfireFilesTransloco } from './external-ngfire-files-transloco';

describe('externalNgfireFilesTransloco', () => {
  it('should work', () => {
    expect(externalNgfireFilesTransloco()).toEqual(
      'external-ngfire-files-transloco'
    );
  });
});
