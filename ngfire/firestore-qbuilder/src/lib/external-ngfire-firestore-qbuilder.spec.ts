import { externalNgfireFirestoreQbuilder } from './external-ngfire-firestore-qbuilder';

describe('externalNgfireFirestoreQbuilder', () => {
  it('should work', () => {
    expect(externalNgfireFirestoreQbuilder()).toEqual(
      'external-ngfire-firestore-qbuilder'
    );
  });
});
