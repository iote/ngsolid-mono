import { externalIoteBricks } from './external-iote-bricks';

describe('externalIoteBricks', () => {
  it('should work', () => {
    expect(externalIoteBricks()).toEqual('external-iote-bricks');
  });
});
