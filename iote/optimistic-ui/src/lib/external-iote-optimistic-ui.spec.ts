import { externalIoteOptimisticUi } from './external-iote-optimistic-ui';

describe('externalIoteOptimisticUi', () => {
  it('should work', () => {
    expect(externalIoteOptimisticUi()).toEqual('external-iote-optimistic-ui');
  });
});
