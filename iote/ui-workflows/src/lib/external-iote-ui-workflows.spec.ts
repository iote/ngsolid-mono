import { externalIoteUiWorkflows } from './external-iote-ui-workflows';

describe('externalIoteUiWorkflows', () => {
  it('should work', () => {
    expect(externalIoteUiWorkflows()).toEqual('external-iote-ui-workflows');
  });
});
